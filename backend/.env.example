MONGODB_URI=
PORT=3000
JWT_SECRET=
JWT_EXPIRES_IN=
FRONTEND_ORIGIN=http://localhost:3001

# Only read by `npm run seed:admin` (never by the running app). Set these
# temporarily to bootstrap the first admin user, then remove them.
ADMIN_EMAIL=
ADMIN_PASSWORD=
